Move columns to the right using .col-md-offset-* classes.
These classes increase the left margin of a column by * columns:

sudo kill -9 $(sudo lsof -t -i:3000)//killing a process on port 3000

#promise format
promise(params)
  .then(resp => { /* This callback is called is promise is resolved */ },
        cause => {/* This callback is called if primise is rejected */})
  .catch(error => { /* This callback is called if an unmanaged error is thrown */ });